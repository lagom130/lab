server:
  port: 7110
spring:
  application:
    name: av-base-guid-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0
logging:
  level:
    root: info
    io.github.lagom130: debug